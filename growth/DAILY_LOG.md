# Growth daily log — one line per session run
# Format: YYYY-MM-DD HH:MM — action. A '<date> DONE' line means the day's goal shipped.
2026-07-21 12:35 — shipped: converter-pair pages (/convert hub + 20 pairs, all 200 live, IndexNow 200) + real BMP/ICO encoders
2026-07-21 DONE
2026-07-21 (manual, pulled from tomorrow) — shipped: RU/UZ converter pages (40, 3-way hreflang, IndexNow)
2026-07-21 16:03 — checked, nothing to resume
2026-07-21 17:05 — shipped: real baseline TIFF encoder (convert-to-tiff no longer emits PNG bytes) | pages:0 | live:yes | next: TIFF converter-pair pages (scope decoder first) or /resize/[preset] pages
2026-07-21 17:52 — shipped: 16 resize-preset pages /resize/<size> + hub (generic resize:WxH engine) | pages:17 | live:yes | next: resize presets batch 2 (~9 more, copy only)
2026-07-21 18:20 — shipped: resize presets batch 2 (9 more sizes, 25 presets / 26 URLs) | pages:9 | live:yes | next: RU/UZ twins for /resize (copy convert-pairs-i18n pattern, +50 pages)
2026-07-21 19:12 — shipped: RU/UZ twins for /resize/<preset> (50 localized pages, 3-way hreflang reciprocal, sitemap 738, IndexNow 200) | pages:50 | live:yes | next: RU/UZ twins for the /resize hub (localized parent for the 50 new pages)
2026-07-21 20:58 — shipped: TIFF converter pairs + client-side TIFF decoder with capability guard (M114) | pages:18 | live:yes | next: RU/UZ twins for the /resize hub (localized parent for the 50 M111 pages)
2026-07-21 21:36 — shipped: RU/UZ hubs for /resize + /convert (M115, 4 localized parents, reciprocal hreflang, live) | pages:4 | live:yes | next: RU/UZ barcode symbology twins
2026-07-21 21:40 — shipped: RU/UZ twins for all 13 barcode symbologies (M116, 26 pages, sitemap 795, false check-digit claim caught pre-deploy) | pages:26 | live:pending-verify | next: RU/UZ hub for /barcode (localized parent for these 26)
2026-07-21 22:05 — shipped: RU/UZ hub for /barcode + 3-level localized breadcrumbs (M117, sitemap 801, verified live) | pages:2 | live:yes | next: soft-404 fix
2026-07-21 22:30 — shipped: dynamicParams=false on 20 routes — unknown params were returning 200 with an empty page across every family incl. EN (M118) | pages:0 | live:verifying | next: audit remaining localized templates (resize/downloader/tool-page i18n) for unsupported claims
2026-07-21 21:55 — shipped: claim audit of every localized template + resize format preservation (M120) | pages:0 (48 downloader + 4 tool + 2 hub pages rewritten) | live:yes | next: fix the preview launch config so design-v2 routes can be verified locally
2026-07-21 23:05 — shipped: canvas output rules extracted to lib/image-output.ts + npm run test:image (23 assertions, mutation-verified) and two more live format bugs fixed — EXIF remover turned WebP into JPG / transparent into black, batch compress encoded every transparent PNG black (M122/M122b) | pages:0 | live:yes (M122 confirmed in deployed chunk; 6 URLs 200) | next: make registry-backed canvas engines drivable in the preview pane (dynamic(ssr:false) chunk stalls there)
2026-07-21 23:40 — shipped: preview pane traps root-caused, registry engines now drivable end to end (M123) | pages:0 | live:n/a (verification tooling, no production code changed) | next: baseFaq() promises PNG output on tools that emit JPG (~40 pages)
2026-07-22 00:35 — shipped: batch-resize + metadata-remover silent format rewrites fixed, format FAQ derived from the real engine (M124) | pages:82 updated | live:yes (magic-byte verified, IndexNow 200) | next: AI Image Upscaler is not AI (owner-gated rename) → skip to usecase-content.i18n.ts audit
2026-07-22 01:05 — shipped: RU/UZ fit/fill + quality + action labels in the sizing engine (M125) | pages:50+ localized | live:yes (driven on production) | next: usecase-content.i18n.ts claim audit (9,228 lines)
2026-07-22 07:25 — shipped: usecase-content.i18n.ts claim audit — 3 false claims × 15 languages fixed, WiFi/iCal payload corruption bug found and fixed with a new mutation-verified suite (M126) | pages:30 (2 use cases × 15 langs) + 4 QR tools | live:yes (verified in EN/RU/DE/JA/TR on production; IndexNow 200 for 65 URLs) | next: client-side PDF compress path so the >4.5 MB files the page is built to sell actually work
2026-07-22 18:20 — shipped: PDF compression moved into the browser — the >4.5 MB files the funnel page is built to sell now work at all, and the on-device claim is true again in 15 languages (M127) | pages:18 updated (tool + RU/UZ twins + 15 use-case landings) | live:yes (driven on production: 0.76 MB → 0.09 MB, 88%, output's image dict matches its bytes exactly, zero /api/pdf requests; IndexNow 200 for 18 URLs) | next: ToolPageShell hardcodes "runs in your browser; files never upload" on every tool page — false wherever a tool POSTs (PDF→Word, AI tools, downloader, merge)
2026-07-22 18:52 — shipped: ToolPageShell's trust strip now derives from the engine — "files never upload" no longer renders on tools that upload (M128) | pages:5 (pdf-to-word EN/RU/UZ, image-to-3d, downloader) | live:yes (verified on production; RU badge drops "в браузере"; compress keeps its now-true on-device claim; IndexNow 200) | next: vCard payload escaping (M129) — ORG "Acme, Inc." or a name with a semicolon still corrupts
2026-07-22 19:35 — shipped: vCard + MECARD payload escaping — a comma in ORG imported as two organisations, a semicolon in a surname shifted the given name into another field (M129) | pages:0 (payload engine + decoder) | live:yes (round trip driven on production: the QR now carries N:Berg\; Jr;John;;; and ORG:Acme\, Inc., decoder renders "John Berg; Jr · Acme, Inc."; test:qr 31 assertions, mutation-verified) | next: internal links off the /url-qr and /vcard-qr 308s (M130)
2026-07-22 19:45 — shipped: 18 internal links repointed off the /url-qr and /vcard-qr 308s — homepage, dashboard, sidebar, category pages, use-case CTAs (M130) | pages:8 files | live:yes (production HTML on /, /pdf-tools, /image-tools, /free-forever and the use-case landings now carries zero href="/url-qr" or "/vcard-qr"; config redirects intentionally kept for external links) | next: poster maker logo upload, or the AI-connector processing flag before any cloud engine is switched on
2026-07-22 19:35 — shipped: /ai-tools privacy claims derive from the connector, gated on a per-engine `wired` flag rather than the env var (M131) | pages:22 AI tool pages | live:yes (NEXT_PUBLIC_AI_ENGINE turned out to be SET on Vercel — the env-keyed first cut made 10 pages announce uploads that never happen; corrected and verified: 12/12 pages back to the true on-device claim, pdf-to-word keeps its cloud strip) | next: monitor.ts reports NEXT_PUBLIC_AI_ENGINE unset as a prod issue while setting it does nothing — make the check assert a wired route
2026-07-22 19:40 — shipped: envValidation reports the connector's real state instead of a flag that was already set and wired to nothing (M132) | pages:0 (health/readiness) | live:yes (/api/ready on production now returns "NEXT_PUBLIC_AI_ENGINE is set but no AI engine routes through the connector" — independent proof the var is set in the prod runtime) | next: poster maker logo upload (makes the review-poster page's strongest claim true again)
2026-07-22 20:35 — shipped: poster maker logo upload + removable "Made with QRix" credit, on a layout that now flows instead of colliding (M133) | pages:18 (poster + guide + 15 use-case twins + sitemap-unchanged) | live:yes (driven on production: 400x200 logo moved the underline 300 → 400 exactly as lib/poster-layout.ts predicts, exported PNG carries the logo with zero footer ink when the credit is off, only /api/v1/track requested; IndexNow 200 for 17 URLs) | next: batch/multi-file conversion for real — AiDropzone takes a single file on every /convert page
2026-07-22 21:15 — shipped: /qr-code-statistics — 26 QR stats, each with its source, its tier and what it does not prove, plus 4 widely-quoted numbers rejected with the reason (M134) | pages:1 new (+sitemap/search/llms.txt) | live:yes (200; canonical + all 5 source hosts in HTML; Article JSON-LD w/ 4 citations + FAQPage(6) + Breadcrumb; sitemap & llms.txt carry it; IndexNow 200; first deploy 404'd 20min on a Server→Client function prop, fixed) | next: CWV audit — Lighthouse on 5 template types, fix to 95+ mobile
2026-07-25 22:20 — shipped: the background canvas stopped owning the main thread — TBT on the tool templates fell from 5.9-9.2s to 120-540ms (M135) | pages:all (layout-level) | live:yes (measured on production with lighthouse mobile: qr-tools/url 51→87, image-tools/compress 41→87, convert/png-to-jpg 45→80, qr-code-statistics 46→83, home 36→55; CSS twin verified in both themes via computed styles) | next: LCP element render delay (~1.4-1.6s) on the tool templates — 75KB LCP font swap and the 29KB render-blocking CSS are the two suspects
2026-07-26 18:05 — shipped: the LCP element on every template was the cookie banner mounting after hydration, plus the consent-state bug next to it (M135 tranche 2) | pages:all (layout-level) | live:yes (observed LCP−FCP gap 532ms→0ms and CLS 0.091→0, each measured twice per side on production; consent verified live in all 3 states — fresh shows the banner, accept persists+updates gtag, reload now boots Consent Mode GRANTED where it booted denied before) | next: TBT — the hydration chunk 2pqvdscfnq65v.js is 1048ms scripting on /qr-tools/url; find what the tool template actually hydrates (gtag is 581ms and already lazyOnload)
2026-07-26 21:40 — shipped: home LCP (M136, three commits) + the tool-page shell moved to the server (M137) | pages:home + all 46 tool routes | live:yes (measured twice per side on production — home obsLCP 6395/3791 -> 1746/2050 ms, LCP-FCP gap 1230/1313 -> 50/266, score 33/36 -> 52/56; /qr-tools/url TBT 2233/644 -> 245/460 ms, hydration chunk 948/1849 -> 483/862 ms scripting, score 49/65 -> 77/72; interactivity re-proved in real headless Chrome — QR canvas re-renders on typing, favorite star stores the right path, zero page errors) | next: TopNav — the root layout still mounts 11 client components on every page and TopNav (400 lines) is the biggest separable one
2026-07-26 23:29 UTC — shipped: three catalogs stopped shipping to every page — the homepage i18n catalog, the Supabase auth SDK and the whole search index left the eager bundle (M138) | pages:all (layout-level) + homepage hero | live:yes (measured as bytes on production, not scores: /qr-tools/url eager script set 19/1405.2 KB -> 18/790.1 KB (-44%), home 22/1539.9 KB -> 21/1281.3 KB (-17%); driven in real headless Chrome — Ctrl+K opens the lazily-loaded palette and "merge pdf" returns 9 rows, Escape closes, second Ctrl+K re-opens, account menu intact, hero bar answers both "jpg to pdf" and "жпг то пдф", zero page errors) | next: the auth SDK and lib/blog are STILL eager on the homepage via ReviewsSection and LatestPosts, both below the fold — same CommandSearchLoader shape, and the homepage is the biggest CWV item left
2026-07-26 23:38 UTC — shipped: ReviewsSection stopped putting the auth SDK in the homepage eager bundle, closing M138 (tranche 6) | pages:homepage | live:yes (homepage eager script set 21/1281.3 KB -> 20/1044.7 KB; end to end today 22/1539.9 -> 20/1044.7 KB, -32%; driven on production — SDK chunk absent from the HTML script set but still requested after load, real /rest/v1/reviews query still fires, zero page errors; also hardened both call sites so a failed chunk fetch degrades to localStorage instead of throwing) | next: lib/blog is the last of the three still eager on the homepage via LatestPosts, but it feeds what that section PAINTS, so it needs a viewport-driven deferral rather than the intent-driven shape used so far
2026-07-27 13:20 UTC — shipped: the 88 KB blog catalog left the homepage's eager bundle — by INLINING the three cards, not by deferring them (M139) | pages:homepage | live:yes (homepage eager set 20 scripts / 1044.7 KB -> 19 / 970.4 KB, marker YES -> no, and the 74.6 KB chunk now 404s — gone from the graph, not deferred; driven in real headless Chrome: all 3 cards render with right title/category/read-time, hydrated, the 3 /blog/ links still in the server HTML and all 3 return 200, zero page errors; no sitemap change so no IndexNow) | next: TopNav markup split — the root layout still mounts 10 client components on every page and TopNav (400 lines) is the biggest separable one, same shape as the M137 ToolPageShell split
2026-07-27 12:10 UTC — shipped: /qr-code-statistics became citable — an embeddable card per figure, 26 of them, each carrying its source and its caveat (M140) | pages:26 embed routes (noindex) + copy-embed UI on the stats page | live:yes (200 per card; the card canonicalises to the stat's anchor on the parent page with utm_source=embed so an embed is a real link back; badge/year/source-date/caveat all render) | next: the card ships the whole site into the embedder's page — 15 eager scripts / 727.1 KB for static text (M141)
2026-07-27 18:20 UTC — shipped: the stat card stopped shipping the whole site into someone else's page (M141) | pages:26 embed routes | live:yes (measured on production: eager scripts 15 / 727.1 KB -> 0 / 0.0 KB, whole document 3.3 KB; no TopNav, no consent banner, no gtag.js firing from inside a third party's iframe; npm run test:embed against production = 26 cards at 320px, 0 clipping, 0 with script, slack 8..64px) | next: verified + logged this session (Jul 28); homepage client-boundary split scoped and constrained by `lang` useState — see BACKLOG note
2026-07-28 10:30 UTC — VERIFY: ok — 10 newest URLs checked live. /qr-code-statistics, /pdf-tools, /free-forever, /blog, /about, /barcode all 200 + self-canonical + own (non-homepage) title. /wifi-qr /sms-qr /telegram-qr /whatsapp-qr are 308s BY DESIGN (M142 moved the stub family into next.config redirects) and every target resolves 200: /qr-tools/{wifi,sms,telegram,whatsapp}. robots.txt still serves "Disallow: /p$" (not the bare /p that once blocked 27 pages). Sitemap 809 URLs vs the 801 strategy baseline — +8, sane, no IndexNow delta to submit. M141 independently re-verified live (0 scripts, caveat intact).
2026-07-28 12:05 UTC — shipped: the site can answer "who created it?" — one operator identity, Person+Organization graph, bylines, Article.image, index dates (M145) | pages:/about rewritten + every blog article + the blog index + layout-level Organization on all ~800 pages | live:yes (23/23 test:eeat against production incl. 6 live assertions: /about 200 self-canonical with a Person node whose @id the site-wide Organization.founder resolves to; an AUTOPILOT article renders the rel=author byline and a complete Article node — image + Person author + datePublished; 72 index dates, zero "Invalid Date"; schema logo /icon.png serves image/png; all 6 /about links 200. Sitemap unchanged at 809 so no IndexNow. 17 static assertions, 10 mutations verified, 0 blind spots) | next: the "we tested 20 free QR generators" methodology page — the /free-forever claim that is still unsourced, and the next linkable asset after /qr-code-statistics
2026-07-28 12:20 UTC — closed (no code): the audit's "/convert emits no hreflang" finding is a FALSE POSITIVE | pages:0 | live:verified (convert/resize/barcode/downloader each emit 4 hreflang; /convert/png-to-jpg + /ru/ + /uz/ all 200, /resize/1920x1080 + twins all 200, /barcode + twins all 200) | cause: Next SSR emits camelCase hrefLang= and dateTime=, which is spec-valid since HTML attribute names are case-insensitive — a case-sensitive grep sees zero. Same trap hit this session's own blog-index date check. Monday audit prompt should require case-insensitive verification | next: the "we tested 20 free QR generators" methodology page (top of NOW, needs real testing of 20 live generators — a full mission, not a tail-end task)
2026-07-29 23:10 UTC — VERIFY: ok — 10 live URLs checked (/, /about, /blog, /qr-code-statistics, /free-forever, /barcode, /pdf-tools, /qr-tools/url, /convert/png-to-jpg, /downloader): all 200, all self-canonical, all carrying their own non-homepage title. robots.txt still serves "Disallow: /p$" (not the bare /p that once blocked 27 pages). Sitemap 809 URLs — identical to Jul 28, no delta, nothing to submit to IndexNow. Tooling note for future sessions: this machine's grep has no working -P (locale), and node resolves a bare /tmp to D:\tmp — parse HTML with node fetch + script-stripping instead, which is what the case-insensitivity trap already argued for.
2026-07-29 23:35 UTC — shipped: the repeat visit stopped re-validating everything — every non-font static asset was `max-age=0, must-revalidate`, including the preloaded homepage LCP image and 1.2 MB of pdf.js worker (M146, 4 sub-items) | pages:all (config + layout + 2 components) | live:yes (verified on production: /scenes/bunny-hero.webp, /world-dots.svg, /pdf.worker.min.js, /qrix-logo.png, /qrix-brand-film.mp4 now 30d+SWR; /sdk/qrix.js on its own 600s rule because third parties embed it and max-age is hard freshness; the 3 deliberate exclusions held — fonts still immutable, sw.js and llms.txt still must-revalidate. Oswald: 0 in the served CSS, @font-face 90->80, its woff2 now 404, while all 5 painting families still ship and the homepage h1 is untouched. Font preloads 2->1. All 3 hero imgs now carry width/height. 4 pages 200 with correct h1s. Sitemap unchanged at 809 so no IndexNow) | next: /convert + /resize server-rendered dropzone shell — CONFIRMED live this session, both serve 0 input[type=file] and 0 <label> (h1 + ~550 words of body copy DO render, so it is the tool only), and dynamic(ssr:false) emits nothing during SSR, so the shell must live in the server page outside that boundary
2026-07-29 23:36 UTC — AUDIT-QUALITY NOTE: 2 of M146's 4 findings were misstated by the Jul 28 audit. "Trim 6 font families toward 3" named no family; only ONE (Oswald) was removable for free, and the other five all genuinely paint, so going to 3 is a design decision the audit had no standing to make. "width/height on the 3 hero imgs" was true but inert — two of the three are position:absolute at 100%/100%, so the attributes cannot move layout, and CLS has been 0 since M135. Combined with the Jul 28 hreflang false positive, that is 3 misstated findings in 2 sessions: treat audit output as leads to scope against production, never as work orders.
2026-07-30 00:20 UTC — shipped: image tools stopped serving crawlers a page with no tool on it — /convert/*, /resize/* and /image-tools/[slug] served 0 input[type=file] and 0 <label> (M147) | pages:all image engines (registry-level, 3 route families + RU/UZ twins) | live:yes (served HTML now carries 1 real input[type=file] + 1 <label for> on convert/resize/crop-image/color-picker/batch-convert/collage-maker and the /ru/ + /uz/ twins, with `multiple` set only for batch:/layout: engines; interactivity re-proved in REAL headless Chrome on 5 URLs — shell removed, live dropzone mounted, tool subtree hydrated. Sitemap unchanged at 809, no IndexNow) | note: the audit's proposed fix would not have worked — it assumed "Loading the image workspace…" was in the server HTML, but ssr:false renders neither the component nor its loading fallback during SSR, so nothing was there at all | next: BarcodeClient a11y (label[for]+id on value input, range, checkbox, textarea; human-readable colour preset names) — mirror the WiFi page, which does it correctly
2026-07-30 00:22 UTC — INSTRUMENT: the in-app Browser pane reported M147 as a total production regression (shell stuck in DOM, no React fiber, no dropzone) and was wrong on every count — it runs at viewport 0x0 and does not hydrate tool-page main content. A revert was one step away. What caught it: running the same probe against a CONTROL page the change never touched (/image-tools/compress), which produced the IDENTICAL failure signature. An instrument that reports the same failure for a changed and an unchanged page is measuring itself. scripts/probe-hydration.mjs now exists — real headless Chrome over CDP, zero new dependencies (Chrome's --remote-debugging-port + Node 22's built-in WebSocket). Use it for anything hydration-shaped; do not trust the preview tab. This is the third instrument failure in three sessions (grep -P absent, case-sensitive attribute grep, now this).
2026-07-30 00:55 UTC — shipped: M147's shell speaks RU/UZ and names its own output; gradient stopped asking for a file it cannot use (M147b) | pages:242 URLs affected, 102 of them localized | live:yes (verified on production after deploy AND on a worktree dev server + probe-hydration.mjs on the changed code: 6 URLs EN/RU/UZ each serve 1 input[type=file] + 1 label[for] matching its id + the localized prompt + "Output/Результат/Natija: <target>", gradient serves 0 file inputs and keeps the spinner, toolAreaHydrated:true on all three languages; npm run test:shell 33 assertions, 7 mutations) | correction: M147's claim that ssr:false suppresses the loading fallback server-side is FALSE on Next 16.2.7 — measured three times, incl. gradient's HTML today; the audit's suggested fix would have worked, the shell is merely the sturdier shape | next: BarcodeClient a11y, or the pre-hydration file selection that is swallowed silently (new BACKLOG item)
2026-07-30 01:20 UTC — shipped: npm run kpi — the growth loop can finally read its own Search Console numbers (scripts/gsc-kpi.mjs, service-account JWT signed with node:crypto, zero new dependencies) | pages:0 (tooling) | live:n/a (owner must add the service account as a user on the property; the reader names that exact failure when it 403s, and the key lives outside the repo in ~/.qrix/gsc.json with a .gitignore net) | why: the Friday KPI snapshot and P2's "expand only what already earns impressions" rule both had no data source — the numbers were pasted by hand or not updated at all | next: run it, write the real baseline into SEO_STRATEGY.md, then pick work from the query report instead of from guesses
2026-07-30 23:00 UTC — VERIFY: ok — 10 newest URLs checked live (the M147/M147b image-tool shells + /qr-code-statistics): /convert/png-to-jpg and its /ru/ + /uz/ twins, /resize/1920x1080, /image-tools/{crop-image,color-picker,batch-convert,collage-maker} all 200, all self-canonical, all with their own non-homepage title, and all still serving 1 input[type=file] + 1 label[for] in the server HTML — M147 is holding. /image-tools/gradient-generator correctly serves 0 file inputs (M147b's deliberate exclusion). robots.txt still serves "Disallow: /p$", not the bare "/p" that once blocked 27 pages. Sitemap 810 vs 809 yesterday — the +1 is /blog/merge-pdf-files-free (autopilot, lastmod 2026-07-29), submitted to IndexNow (200; key file 200).
2026-07-30 23:55 UTC — shipped: /free-qr-code-generator-comparison — the "we tested 20 free QR generators" study that /free-forever had been citing for weeks without anyone having run it (M148) | pages:1 new + /free-forever rewritten (+sitemap/search-index/llms.txt) | live:yes (200, self-canonical, own title "We Checked 20 “Free” QR Code Generators — 13 Have a Catch"; Article JSON-LD with 20 citations + FAQPage(6) + Breadcrumb parse clean; 4802 visible words; 21 table rows and 21 vendor cards; 20 outbound vendor links all rel="nofollow noopener"; embedded URL QR generator present; sitemap 810 -> 812 and llms.txt carries it; IndexNow 200. /free-forever now renders "13 of 20 can switch a free code off / 6 of 20 cap free-tier scans / 4 of 20 brand what your scanner sees / 14 of 20 require one / 5 of 20 paywall it" — every cell derived from the dataset — and the fabricated sentence is gone from production HTML) | measured: 13 of 20, NOT the 14 the site asserted. Count deliberately narrow — account walls (14/20) and paywalled vector (5/20) are disclosed at the door and reported separately as friction; the 13 is what bites after you print. The finding worth the page: all 5 static-only generators had nothing that could switch a printed code off and every hosted one had at least one lever — the catch arrives with the hosting, not the company. QRix is in the same table on the same six questions including the row it loses | guard: npm run test:study, 18 assertions, 4 mutations verified (incl. a count hardcoded into JSX instead of read from COUNTS, which is the exact failure this mission undoes) | next: BarcodeClient a11y, or the pre-hydration file selection swallowed silently (both in NOW)
2026-07-30 23:58 UTC — METHOD NOTE, and it nearly shipped wrong: the two sharpest accusations on the study page were both read off tag-stripped text, where a tick/cross sits adjacent to a label and column attribution is genuinely ambiguous — exactly the trap the Jul 28 hreflang false positive and the Jul 29 grep -c lesson already described, arrived at from a new direction. Re-checked in raw markup before trusting them: The QR Code Generator's $0 Free card carries <img alt="Does not contain Feature"> on its "Watermark-free QR codes" row (the paid card says "Contains Feature"), so the vendor's own alt text settles it; and ME-QR turned out to publish something stronger than what had been cited — its FAQ schema says the free version differs from Premium "in the presence of ads after scanning", direct rather than inferred. Both verdicts held. Rule for any future comparison work: a claim about a named company may not rest on flattened HTML. Parse, or read the raw node.
2026-07-31 00:05 UTC — shipped: the barcode tool got accessible names, and stopped being English on its RU/UZ twins (M149) | pages:all /barcode + /ru/barcode/* + /uz/barcode/* (13 symbologies x 3 languages) | live:yes (verified on production in real headless Chrome via the new scripts/probe-barcode.mjs on RU and UZ: 5/5 controls present and hydrated, barcode paints 31 and 38 rects, a label click toggles its checkbox so htmlFor/id really pair in a browser, every control resolves an accessible name, 0 swatches named by hex, typing re-renders the code — UZ code-128 went 38 -> 35 rects — 0 page errors. Server HTML on all three languages: 7/7 expected strings, 5 label[for] with 0 orphans, 8 language-namespaced ids, aria-invalid present, no cross-language leakage) | two defects, one JSX: the audit's a11y item (range, custom colour input and bulk textarea had NO accessible name; swatches announced "#7c3aed") and the larger one found while scoping it — LocalizedBarcodePage never passed lang and BarcodeClient took none, so every RU/UZ barcode page wrapped a fully English tool. barcodeUI() covered only the page chrome, which is why it survived two localization passes | guard: npm run test:barcode, 8 assertions, 4 mutations verified; the load-bearing one asserts RU/UZ != EN, because an untranslated string is invisible unless something compares the languages to each other | sitemap unchanged, so no IndexNow | next: the fourth occurrence, now scoped — see below
2026-07-31 00:10 UTC — SWEEP (post-M149, cheap, and it hit): the English-only client tool has now happened four times, not three. components/LocalizedToolEngine.tsx renders EIGHT clients — PdfToWordClient, MergePdfClient, CompressPdfClient, JpgToPdfClient, PdfToJpgClient, RemoveBgClient, ImageUpscaleClient, ImageToTextClient — and passes lang to none; none accepts one. It backs every /ru/[tool] and /uz/[tool] route, i.e. the main PDF and image surface for the RU/UZ audience, which is the stickiest audience the site has. The file's own header comment carries the assumption that hid it: "the tools are language-agnostic, only the surrounding SEO copy is localized" — the first half is false. Written up as a NOW item with two traps recorded in advance: (1) all eight are dynamic(ssr:false), so curl cannot see the defect OR the fix — use the CDP probe, same instrument M147 proved necessary; (2) ImageToTextClient already has a `lang` state but it is Tesseract's OCR recognition language, not a UI locale. Method for reuse: ls components/Localized*.tsx, grep each for a client render, check for lang=. Six wrappers, four render no client, one was M149, one is the hit.
2026-07-31 00:12 UTC — METHOD NOTE (M148): two claims about named companies nearly shipped on tag-stripped HTML, where a tick sits adjacent to a label and column attribution is genuinely ambiguous. Re-read in raw markup before trusting: The QR Code Generator's $0 Free card carries <img alt="Does not contain Feature"> on its watermark row while the paid card says "Contains Feature", and ME-QR publishes a stronger statement than the one cited (its FAQ schema says the free version differs from Premium "in the presence of ads after scanning"). Both verdicts held. Standing rule from here: a claim about a named third party may not rest on flattened HTML — parse it, or read the raw node. This is the fourth instrument failure in four sessions (grep -P absent, case-sensitive attribute grep, the 0x0 preview pane, now this).
2026-08-01 11:35 UTC — VERIFY: ok — 10 live URLs checked (/free-qr-code-generator-comparison, /free-forever, /barcode, /ru/barcode/code-128, /uz/barcode/code-128, /qr-code-statistics, /about, /convert/png-to-jpg, /image-tools/crop-image, /resize/1920x1080): all 200, all self-canonical, all carrying their own non-homepage title, and the RU/UZ barcode twins still serve their localized titles (M149 holding). robots.txt still serves "Disallow: /p$", not the bare "/p" that once blocked 27 pages. Sitemap 812 URLs — identical to Jul 30's post-M148 count, no delta, nothing to submit to IndexNow.
2026-08-01 12:55 UTC — shipped: the fourth English-only tool defect, closed eight tools wide — every /ru/[tool] and /uz/[tool] route had been wrapping a fully English tool (M150, 3 commits) | pages:all localized PDF + image tool routes (8 tools x RU/UZ) | live:yes (13/13 URLs verified on production in real headless Chrome via the new scripts/probe-tool-i18n.mjs: RU+UZ merge/compress/jpg-to-pdf/pdf-to-jpg/background-remover/image-upscaler/image-to-text/pdf-to-word all render their localized strings with 0 English leaked, tool subtree hydrated, 0 page errors — plus /pdf-tools/merge as an English control proving the change left EN untouched. Sitemap unchanged at 812, so no IndexNow) | two sub-defects found while scoping: RemoveBgClient used its English colour name as BOTH the accessible name and the download filename suffix (split into an ASCII key + localized label; swatches also gained a real aria-label, they had none), and ImageToTextClient's `lang` state was Tesseract's OCR language — renamed ocrLang so it cannot be mistaken for the UI locale again | guard: npm run test:tool-i18n, 34 assertions, 14 mutations verified. The engine assertion now parses EVERY case in the switch rather than a hand-listed subset, so a ninth client added unwired fails immediately — the subset shape is exactly what let this survive two localization passes | next: /free-forever's unmeasured PROMISES card (small, same family as M148), or localizing the study
2026-08-01 12:58 UTC — INSTRUMENT NOTE, and this session broke its own tools four times. (1) I ran a mutation test with `git checkout --` on UNCOMMITTED work and destroyed three files' edits — commit BEFORE mutation-testing, always. (2) The guard's comment-stripper used a naive block-comment regex; the literal in accept="image/*" opened a match that ran to the next real close-comment token and ate 2.3 KB of JSX, so a genuinely re-hardcoded "Page size" passed clean. Now bounded by counting t.<section>. references before/after — a ratio bound is useless because PdfToWordClient is legitimately 22% comments. (3) `grep -c FAIL` reported a CRASHED test script as zero failures, so a whole mutation batch measured nothing; the runner now checks the exit code and greps for SyntaxError first. (4) The CDP probe reported /pdf-tools/merge as unhydrated because ToolPageShell emits no <main>, so the selector returned null and the fiber scan examined nothing — caught only by running the same probe against a control page the change never touched, which is the M147 lesson arriving for the second time. Standing rule reinforced: an instrument that reports the same result for a changed and an unchanged input is measuring itself.
2026-08-01 12:32 UTC — shipped: the last unmeasured comparative claim on /free-forever, and the "15 languages" overstatement found under it (M151) | pages:/free-forever | live:yes (verified on production after deploy: 200, self-canonical, own title; the card now reads "Print-ready SVG export, bulk CSV generation and the QR design studio cost nothing here, with navigation in 15 languages. Vector is the one we measured against others: 5 of 20 paywall it"; the old heading "Free features others charge for" returns 0 occurrences; all five M148 table rows regression-checked intact — 13/6/4/14/5 of 20 — and the study link still resolves) | what it was: the PROMISES card claimed four things competitors charge for while the study had measured exactly one of them (vector). Narrowed rather than re-measured on purpose — "has a design studio" has no yes/no reading the way "is vector free?" does, so a re-sweep would have produced softer claims than the ones it replaced. Every remaining clause is now a claim about QRix alone and each was verified in the repo first (SVG export in QRGenerator + QRDesignStudio, /bulk-qr takes CSV/TXT with no auth gate, QRDesignStudio.tsx is the studio) | the sharper half, found while checking the easy part: "15 languages" was also overstated. SITE_LANGS is exactly 15 and TopNav + the homepage really render all 15 (NAV_I18N 12 + en/ru/uz base), but tool UI copy is EN/RU/UZ only — the very thing M149 and M150 spent two missions establishing. Now read from SITE_LANGS.length and scoped to "navigation", because a reader who switches to Japanese gets Japanese nav and an English tool | guard: npm run test:study 19 assertions, 5 mutations verified (old heading returns, language count hardcoded, COUNTS dropped from the comparative clause, language claim unscoped, a card deleted). It asserts on the CARDS specifically — M148 cleaned the table while the same defect sat one screen higher — and counts the cards before asserting anything, because M150 shipped a comment-stripper that ate 2.3 KB of JSX and let every assertion behind it pass vacuously | sitemap 812 -> 814 (today's autopilot post /blog/extract-text-from-image-ocr, live 200); it plus the changed /free-forever submitted to IndexNow (200, key file 200) | next: /compare/[slug] — 3 pages, 21 hand-typed comparative cells against named vendors incl. competitor prices, no source cited anywhere
2026-08-01 12:33 UTC — METHOD NOTE (M151), and this time the check paid off in the other direction. Filing the /compare/[slug] follow-up, I was one sentence from writing that its FAQ claim "verified side-by-side against iLovePDF output on real documents" was a fabrication of the same family as M148's invented study. It is not: progress.md M96 records exactly that comparison — both DOCX outputs dissected, rendered through Word and rasterized. It needs a citation, not a retraction. After three misstated audit findings in recent sessions (the hreflang false positive, the font-family item, the inert width/height item) the reflex had become "assume the unsourced claim is invented"; that reflex is itself an instrument, and it fails in both directions. The rule that held: check the repo's own history before accusing the repo of fabricating.
2026-08-01 12:52 UTC — shipped: the competitor column on /compare/[slug] is sourced, and three of its cells were factually wrong (M152, 3 commits) | pages:3 (/compare/qrix-vs-{ilovepdf,tinywow,snaptik}) | live:yes (all three verified on production: 200, self-canonical, own non-homepage title; each carries its vendor source link with rel="nofollow noopener" — ilovepdf.com/pricing, tinywow.com/pricing, snaptik.app — plus a read-date of 2026-08-01; "not stated" markers render on all three; every old wrong claim returns 0 occurrences, the only "pop-under" left being our own column saying we have none) | THE FINDING: 21 head-to-head cells about NAMED products, hand-typed, citing nothing. Reading the vendors' own pages found three wrong. iLovePDF — we asserted "Limited tasks/day on free tier"; its pricing page states NO daily task cap, the limit it states is file size per task (Merge/Split 100 MB, Compress 200 MB vs 4 GB) and its own Batch processing row reads Unlimited for free and paid alike (the $4-7/mo price we quoted was correct: 4 US$/mo annual, 7 US$/mo monthly). TinyWow — we priced ad-free at "~$6/month"; its page lists 20 US$/mo or 15 US$/mo yearly, and the ~$6 looks like its GBP category plan (£5.99) read as the USD ad-free one; its page DOES back our ads row in its own words ("No advertisements" is the first Premium benefit, next to "Skip all CAPTCHAs"). SnapTik — we credited it with MP3 support it explicitly declines ("respects the intellectual property rights of the tracks") and downgraded its photo support to "Partial" when it states it merges slideshows into MP4 automatically: wrong in its favour on one cell and against it on the other, which is the signature of cells nobody checked | also removed: pop-unders / fake Download buttons / ad gauntlet, asserted for months about a named site. A fetched page cannot establish what an ad slot fills with later, so the page now reports what the markup shows (3 script tags, one external host, an ad slot) and says the rest was not measured. 8 of 21 cells render "not stated" instead of a guess | kept after checking: the PDF-to-Word side-by-side-against-iLovePDF claim is REAL (M96) — reworded to say it was our own testing, not retracted | guard: npm run test:compare, 10 assertions, 8 mutations verified | sitemap unchanged at 814; the 3 changed URLs submitted to IndexNow (200) | next: nothing re-reads either vendor dataset — the "Re-check date" NOW item should cover both lib/qr-generator-study.ts and lib/compare-sources.ts
2026-08-01 12:53 UTC — METHOD NOTE (M152): the ~$6 error is what reading flattened text produces. iLovePDF's prices exist only in data-pricing attributes and TinyWow's plan list is embedded JSON — neither survives tag-stripping, and TinyWow's JSON carries GBP rows next to USD ones, so a flattened read picks up whichever number is nearest. This is the third session in a row where the M148 rule (a claim about a named third party may not rest on flattened HTML — parse it, or read the raw node) has paid, and the first where it caught an error already shipped rather than one about to ship.
2026-08-01 16:35 UTC — shipped: the cited pages can be re-read now, and the first pass found a wrong cell (M153, 3 commits) | pages:/free-forever + /free-qr-code-generator-comparison + /compare/qrix-vs-snaptik | live:yes (verified on production after deploy: /free-forever now renders "7 of 20 cap free-tier scans" — it read 6 before this deploy, checked immediately prior — while the other four counts hold at 13/4/14/5 of 20; the study page returns 0 occurrences of both old Unitag claims ("unlimited scans with no per-scan fees", "Free gets you a 1200") and carries the new copy; TWO distinct read-dates now render, 2026-07-30 for the sweep and 2026-08-01 for Unitag's re-read; all three URLs 200 + self-canonical; sitemap unchanged at 814 so nothing new, but the 3 CHANGED URLs submitted to IndexNow (200, key file 200); npm run recheck:sources re-run against the deployed site = 24 sources, 50 markers, 0 moved) | WHAT IT IS: neither vendor dataset stored anything a machine could re-check — `note` is prose, a paraphrase, so there was no string to search for. Every source now carries `evidence`, literal substrings copied out of the live page, and scripts/recheck-sources.mjs re-fetches all 24 and reports which vanished. It REPORTS, never re-grades; unreachable is a separate outcome from moved; matching is raw markup, whitespace-normalised, case-insensitive (one marker is an alt attribute, because "Watermark-free QR codes" sits beside a cross on the free card and a tick on the paid one) | THE FINDING: UNITAG had three wrong cells. Its "unlimited scans" belongs to a €12 paid HD offer and we read it as a property of every plan — the same flattened-read cause as M152's ~$6. Its FAQ, asked directly, says free codes "will stop working after being scanned a hundred times", so we had recorded NO scan cap on the vendor with the HARDEST cap in the study, wrong in its favour; plus an expiry question marked unanswered that the FAQ answers outright; plus a headline crediting the free tier with the 1200×1200 PNG that is the paid download (free is 300px). COUNTS.scanCapped 6 -> 7 and both pages followed with no edit, because they read the dataset — the machinery M148 built. The 13 held: Unitag was already counted, on its dynamic-code limit | ALSO: SnapTik's MP3 row was HALF A SENTENCE. Its FAQ declines MP3 and then says audio IS downloadable via its Download Audio button; we quoted only the refusal, which reads as "no audio at all" — wrong AGAINST the vendor, the mirror of the error M152 fixed in their favour. test:compare now requires both clauses | guard: npm run test:recheck, 10 assertions, 9 mutations verified. Two markers were rejected by its own length rule while being written ("Forever Free", "No Watermark" — marketing fragments that would match boilerplate forever) | KNOWN LIMIT, recorded not papered over: the guard is STRUCTURAL. It proves every source can be re-checked; it cannot prove a verdict matches its evidence. Reverting Unitag's cell today would pass every test — only a human re-reading the page catches that, which is what this pass was | next: wire recheck:sources into the daily VERIFY pass (new NOW item; the routine lives in the owner's scheduled-task file, so the interim is running it whenever a session touches either dataset)
2026-08-01 16:36 UTC — INSTRUMENT NOTE (M153), and for once the instrument caught its own author. The FIRST run of recheck:sources failed exactly one marker, and it was mine: I had stored Uniqode's deactivation policy in THE-QRCODE-GENERATOR.COM's wording. Both pages describe the same policy — tqrcg's comparison page says "all dynamic QR Codes you created will become inactive" about Uniqode, while Uniqode's own page says "any" — and I had copied the sentence from the wrong tab. A vendor's policy as described by its competitor is not the vendor stating it, and the checker is what noticed. test:recheck now forbids the same marker appearing under two vendors for that reason. Second instrument note, same session: `grep -o "read 2026-08-01"` reported the new per-row date as ABSENT on a page that renders it, because Next injects `<!-- -->` between adjacent text nodes — the fourth variant of the flattened/naive-match trap (grep -P absent, case-sensitive attributes, tag-stripping, now comment injection). Match with the comment marker optional, or parse. Third: node resolves a bare `/tmp` path to D:\tmp on this machine, which is already in the Jul 29 note and bit again — fetch in-process instead of shelling out to a temp file.
2026-08-01 16:52 UTC — shipped: the daily VERIFY pass is one command instead of a prose checklist (M154, bf09a43) | pages:0 (tooling) | live:n/a — but it RUNS against production and its first run is green: sitemap 814 URLs (76 dated), robots.txt serving the anchored `Disallow: /p$` and not the bare `/p`, the 10 newest URLs all 200 + self-canonical + their own non-homepage title, 24 cited vendor sources / 50 markers / 0 moved | why: the pass was prose in the routine, re-executed from memory by each session, which is why its rigour visibly varies across this log — and it is the exact shape of the defect it exists to catch, since "check robots is still right" is an intention, not a check | THE DESIGN POINT, reusable: the two rules that cannot be proved against production live in scripts/verify-rules.mjs, pure and separately tested, BECAUSE PRODUCTION IS HEALTHY — running the real pass only ever exercises the happy path and can never distinguish a working rule from a broken one. The robots rule proves it: the bad value is a strict PREFIX of the good one, so the obvious `body.includes("Disallow: /p")` is true on the fixed file AND the broken one, and would have reported the 27-page outage as healthy. test:verify feeds it that exact file, plus the subtler shape where both lines are present | CORRECTION to the plan, found while building: only 76 of 814 URLs carry a lastmod (the autopilot posts), and `undefined` sorts FIRST under localeCompare, so newest-by-lastmod alone would have silently spot-checked twelve arbitrary /use/* pages. Target set is now newest-by-lastmod UNION everything new since the committed snapshot — the second half needs no lastmod and is the honest half | the baseline updates only on a clean run: one that advances through a regression reports a lost page once and then calls it normal | guard: npm run test:verify, 14 assertions, 8 mutations verified. Its own first run failed an over-strict assertion that matched the runner's SUCCESS MESSAGE — a guard tripping over prose about itself, the M150 comment-stripper lesson from a new direction; it now matches the logic, not the words | next: [B] one line in the owner's scheduled-task file pointing the VERIFY step at `npm run verify:daily`; until then sessions run it by hand as the first act of each UTC day
2026-08-01 21:55 UTC — shipped: the Design Studio arrives on intent, and two instruments were lying (M155, 5 commits) | pages:0 (perf — the homepage + all 40 /qr-tools/* routes) | live:yes (measured on production before and after: / 859.9 -> 840.7 KB and 18 -> 17 eager scripts, /qr-tools/url 680.1 -> 661.0 KB; the "Classy R." marker YES -> no on both; probe:studio green three consecutive runs on both URLs — warm 500 ms, open 250 ms, reopen ok, 3/3 studio markers, live canvas + colour inputs, zero page errors; / + /qr-tools/url + /qr-tools/wifi all 200 and self-canonical; sitemap unchanged at 814 so no IndexNow) | ATTRIBUTION FIRST, and it moved the target off the homepage split the item had queued: QRDesignStudio was eager on the two templates that matter most for a MODAL that only a click can open, its own heavy libs (qr-code-styling, jsqr, jspdf) already deferred inside it. Warmed on pointerenter/focus so the click does not stall, cached at module scope, and a failed chunk is a visible retry rather than a dead button. | THE HONEST NUMBER: the studio's 36.5 KB chunk left the eager set entirely but Turbopack rebalanced ~17.3 KB of genuinely shared code into a chunk the page still loads, so the win is -19.2 KB, not -36.5. Predicting from the chunk list and reporting from the diff are different things. | Guards: test:layout now 17 assertions / 7 mutations, plus probe:studio. | next: the modal-a11y sweep (focus trap, Escape, focus restoration across every dialog in the site) or the click-gated re-audit of the other 46 tool routes — both filed at the top of NOW.
2026-08-01 21:56 UTC — INSTRUMENT NOTE (M155), and this session's whole shape was instruments failing in the direction that costs most: reporting a WORKING build as broken. Five separate ones. measure-eager-bundle had been counting the 110 KB noModule bundle no module-capable browser fetches, so every absolute figure the CWV item records was that much too high (the deltas held) — hidden behind the camelCase-attribute trap for the FOURTH time, since React SSR emits noModule="" and a grep for "nomodule" finds nothing. Then four in the new probe: a synthetic pointerenter React never listens for (it derives onPointerEnter from the bubbling pointerover at the root); a pre-hover snapshot diff racing Next's prefetcher; in-page fetch() competing with the page for connections and running out of budget after 13 of 25 chunks; and finally the one worth remembering — the homepage's QR card LEVITATES, so a centre measured at one instant has drifted by the time CDP dispatches the move, the pointer lands beside the button, and no crossing means no warm. It asks trigger.matches(":hover") and re-aims now. Two rules earned twice over: prefer the question that has no race in it to the one that needs a baseline, and wait for the condition, not the clock.
2026-08-02 01:58 UTC — VERIFY: ok — npm run verify:daily, green on every check: sitemap 814 URLs (76 dated), 814 -> 814 since 2026-08-01 with none lost; robots.txt still serving the anchored `Disallow: /p$` and not the bare `/p` that once blocked 27 pages; the 10 newest URLs (the autopilot blog set — extract-text-from-image-ocr, upscale-image-quality-ai-free, remove-background-from-image-free, merge-pdf-files-free, compress-pdf-without-losing-quality, convert-pdf-to-word-keep-formatting, instagram-reels-download-mp3, tiktok-video-download-without-watermark, ai-image-denoiser-online, image-to-3d-model-online-free) all 200 + self-canonical + own non-homepage title; 24 cited vendor sources / 50 markers / 0 moved / 0 unreachable / 0 older than 120d. No sitemap delta, so nothing to submit to IndexNow. First day the pass ran as one command rather than from prose — it is M154 doing what it was built for.
2026-08-02 02:00 UTC — shipped: M156 confirmed live — the tool shell no longer offers a file picker it cannot honour (M156b, verification half) | pages:every /convert/*, /resize/* and /image-tools/* route behind ImageEngineRegistry, EN/RU/UZ | live:yes (all three recorded check URLs 200 and now serving `disabled="" aria-describedby="image-tool-status"` on input#image-tool-file, against a recorded pre-deploy baseline of NO disabled attribute anywhere — so the diff is the fix and nothing else; each twin serving its own status copy, EN "Preparing the tool — one moment." / RU "Инструмент готовится — одну секунду." / UZ "Asbob tayyorlanmoqda — bir lahza.", with the English string returning 0 occurrences on both localized pages; label[for] still pairing on all three, so crawler-facing markup is unchanged as designed) | THE CHECK THE ITEM DID NOT WRITE DOWN, and the only one that could have failed: curl sees the pre-hydration state ONLY, so a control accidentally left disabled after the engine mounts passes every assertion the item specified while being a worse bug than the one being fixed. probe-hydration.mjs (real headless Chrome over CDP) on the same three URLs post-deploy: toolAreaHydrated true, liveDropzone 1, hiddenFileInputs 1, loadingFallbackVisible false, shellStillPresent FALSE — the busy shell is discarded outright when the engine takes over, so the disabled state cannot outlive the window it describes | guard: npm run test:shell 39/39 on the deployed tree | sitemap unchanged at 814, no IndexNow | next: modal a11y beyond the announcement (focus trap / Escape / focus restoration, QRDesignStudio is only the first case), or the re-audit of which OTHER click-gated components still ship eagerly — both at the top of NOW
2026-08-02 03:05 UTC — shipped: every dialog on the site got Escape, a focus trap and focus restoration, from one shared hook (M157, 4 commits) | pages:0 new (behaviour — the homepage, all 40 /qr-tools/* routes, the command palette on all ~814 pages, the dashboard drawer and the AI before/after viewer) | live:yes (probe:modal-a11y in real headless Chrome on production, and EVERY FIELD FLIPPED against the baseline the same instrument recorded on the pre-deploy build: focusMovedIn false->true, tabsHeldInside 0->10, escapeClosed false->true, focusRestored false->true, 0 page errors. Green on / and /qr-tools/{url,wifi,vcard}. Regression check because this touched both studio files: probe:studio still 2/2 — warm, 3/3 markers, 12 canvases, reopen ok, 0 errors) | THE SWEEP FIRST, as the item instructed, and it is why this found more than it was filed for: grepping role="dialog" only finds dialogs that already know they are dialogs, which is the blind spot that created the item. Swept the OVERLAY SHAPE instead (`fixed inset-0`, non-decorative) and found SIX modals with five different answers — QRDesignStudio and the dashboard drawer had the ARIA and nothing behind it, CommandSearch had Escape but no trap, and AiKit's fullscreen viewer plus DesignPanel had NO ROLE AT ALL. CookieConsent was deliberately left alone: it is a banner, not a modal, and a focus trap on something that appears at page load would be hostile | THE TRAP AVOIDED BY READING BEFORE WRITING: CommandSearch binds Tab to CYCLE ITS FILTERS and calls preventDefault. An unconditional focus trap would have silently deleted a working feature. The hook ignores e.defaultPrevented for exactly that — React attaches at the root container, so the input's handler runs before the window listener and the trap stands down | DEAD CODE, recorded not deleted: components/DesignPanel.tsx has zero importers anywhere. The guard allowlists it WITH a re-check that it is still unimported, so the exemption dies the day anything uses it | guard: npm run test:modal-a11y, 46 assertions, 11 mutations verified. It REDOES THE SWEEP every run instead of asserting over a hand-listed set (the M150 lesson), and earned that immediately — it FAILED on first run against QRDesignStudioLoader, which I had not wired: a dialog containing no focusable control at all, where Escape is the only exit and there was none | sitemap unchanged at 814, no IndexNow | next: the click-gated re-audit (scoped below during the deploy wait — it looks smaller than filed), or the CWV epic's homepage split
2026-08-02 03:06 UTC — INSTRUMENT NOTE (M157), three, and two of them cost real time. (1) The probe's first run reported "the studio never opened" on a build where opening works: Chrome performs a key's DEFAULT ACTION only for a keyDown carrying text, and Enter had been sent as rawKeyDown, so the button was never pressed. An instrument failure wearing a finding's costume — the same direction of failure as all four of M155's. (2) Worse, it then reported three independent-looking failures downstream ("focus left on Tab #1", "Escape did not close") about a dialog that was never open; a prerequisite failure now throws instead of cascading, because three noisy lines read like three findings. (3) The marker I chose to watch for the production deploy — the md5 of the page's chunk-filename set — was IDENTICAL before and after the deploy, because Turbopack chunk names here are not per-build hashed. It could not distinguish the two builds at all, which is the standing rule ("an instrument that reports the same result for a changed and an unchanged input is measuring itself") arriving from yet another direction. Use the Vercel deployment API's readyState + target:"production"; the GitHub commit status says "completed" for the design-v2 PREVIEW while production is still building, and the preview is behind SSO so it 302s and cannot be probed.
2026-08-02 03:20 UTC — INSTRUMENT NOTE (M157, addendum) and the deploy-watcher failed a THIRD way, worse than the two already logged. Left running after the verification was done by other means, it eventually printed "NEW BUILD LIVE after 2280s (chunkset d41d8cd98f00b204e9800998ecf8427e)" — and that hash is the md5 of the EMPTY STRING. A transient curl returned nothing, the empty result differed from the baseline, and the watcher read "different" as "the new build is live". So the same instrument (a) could not distinguish the two builds when it worked, because Turbopack chunk names here are not per-build hashed, and (b) reported success loudest at the exact moment it measured nothing at all. Both halves are the same root error: it compared a fingerprint without ever checking the fingerprint was VALID. Rule for any future poll-until-changed watcher: assert the measurement is non-empty and well-formed BEFORE comparing it, and prefer a signal that states the thing you want (deployment readyState + target:"production") over one that merely differs from a baseline.
2026-08-02 03:25 UTC — INCIDENT (M158), and this worker probably caused it: production is serving a VERCEL SECURITY CHALLENGE to every non-browser client. Every path including /robots.txt and /sitemap.xml answers 403 with `X-Vercel-Mitigated: challenge` and a "Vercel Security Checkpoint" body, UA-independently (plain curl, Chrome UA and a spoofed Googlebot UA all 403 — the last proves nothing, since Vercel verifies crawlers by IP/rDNS and a fake Googlebot from a random IP should be challenged). Not IP-local: WebFetch, on a different network, is also 403. REAL BROWSERS ARE UNAFFECTED — probe:modal-a11y and probe:studio drove real headless Chrome through these same URLs at ~02:50 and got the real pages, so this is not an outage and M157's live verification stands. TIMELINE POINTS AT ME: verify:daily curled 10 URLs fine at 01:54; the Vercel project's updatedAt is 02:41:13; first observed 403 at 02:59 — and between 02:40 and 02:55 this session ran three headless-Chrome probe sessions plus dozens of curls at a handful of URLs, while a deploy watcher polled production every 30s for 38 minutes. Most likely Vercel's automatic attack mitigation tripped on that burst; the alternative (the owner enabled Attack Challenge Mode by hand at 02:41) is not excluded and I cannot tell from here. NOT ACTED ON: Attack Challenge Mode is a security setting on the owner's project and turning it off is not a growth worker's call. Filed [B] at the top of NOW with the exact place to look and the one-line re-test (`curl -sI https://qrixtools.com/robots.txt | grep -i x-vercel-mitigated`). CONSEQUENCE FOR THE TOOLING: verify:daily, recheck:sources and measure-eager-bundle will all report failures that are not real defects until it clears — do not "fix" what they report. THE LESSON, independent of cause: the verification habit this repo has built (probe production, curl a dozen URLs, re-run three times for confidence) is itself a traffic pattern, and it has now been mistaken for an attack. Space runs out, prefer one probe run where the question does not need three, and never point a 30s poll loop at production.
2026-08-02 03:26 UTC — parked: the click-gated re-audit (M158) was taken and put straight back, NO CODE TOUCHED, marker reverted [~] -> [ ]. Its byte-attribution half needs measure-eager-bundle, which fetches over plain HTTP and therefore now measures the challenge page: it reported "0 eager scripts, 0.0 KB" on /pdf-tools/merge, /image-tools/compress and /convert/png-to-jpg with every marker absent — including the nav-label marker its own comment says must always be present. That inconsistency is what exposed it; three Next.js templates appearing to ship zero JavaScript is not a finding, it is an instrument reading a 403. Follow-up recorded on the item: the script should fail loudly on a non-200 or a zero-script page, because today "measured nothing" and "there is nothing" print identically.
2026-08-02 03:36 UTC — RESOLVED, and the resolution is also the diagnosis: the Vercel challenge CLEARED BY ITSELF after ~8 minutes of no traffic from this machine — /robots.txt and /sitemap.xml both 200, no `x-vercel-mitigated` header, total duration roughly 20 minutes. Attack Challenge Mode is a manual toggle and does not expire on its own, so this was Vercel's AUTOMATIC rate-triggered mitigation, which settles the question the incident note left open: the owner did not enable anything at 02:41; this session's own verification burst tripped it, and the project's updatedAt is Vercel writing the mitigation state. NOTHING FOR THE OWNER TO CHANGE — the [B] is closed [x] and the push notification I sent at 03:26 asking them to check the security settings was PREMATURE. The correct order was one quiet re-test, then escalate; a condition that clears itself after a lull is exactly what a rate-limit mitigation looks like, and I escalated before testing that. Worth one GSC Coverage glance at the next KPI pass in case a crawl landed inside the window, but a 20-minute gap is a retry for Googlebot, not a deindexing event. THE DURABLE LESSON IS UNCHANGED and it is the one worth keeping: this repo's verification habit — probe production, curl a dozen URLs, re-run three times for confidence, poll a deploy every 30s for 38 minutes — is a traffic pattern, and it has now been mistaken for an attack by the host. Space the runs, prefer one probe run where the question does not need three, and never point a 30s poll loop at production.
2026-08-02 13:40 UTC — shipped: pdf-lib loads on intent, not on page load — ~413 KB off every PDF tool route (M159, 6 commits) | pages:0 new (behaviour — ~20 /pdf-tools/* routes) | live:yes (measured on production after deploy: merge 1065.0 -> 651.6 KB, split 1064.8 -> 651.5, rotate 1047.0 -> 633.6, watermark 634.1, protect 634.2 — every PDF route now sits with the image and convert templates, which is where /pdf-tools/compress already was. Controls unmoved: /qr-tools/url 662.3, /image-tools/compress 634.2. All routes 200, self-canonical, own titles. probe:pdf-defer 6/6 green in real headless Chrome, 0 page errors) | THE SOURCE SWEEP MISSED IT, and why is the durable part: last session's grep looked for `{open && <Heavy/>}` and concluded the public tool routes had nothing to win. pdf-lib is click-gated with NO boolean anywhere — the gate is that the function cannot run until a file exists — so no grep for a render condition could ever find it. Byte attribution found it in one command, and /pdf-tools/compress was the tell: the ONE PDF client that already used import() sat 400 KB below its eleven siblings. M155's rule was "eager no matter how it is RENDERED"; the real rule is eager no matter how it is REACHED. Attribute first, grep second | guard: test:layout 22 -> 33, and one new assertion SURVIVED its first mutation — `/pdfLib = null;/` matches `// pdfLib = null;`, the identical substring trap M155 recorded — anchored and re-mutated three ways | next: /embed/downloader second-root-layout item, or the homepage split (the ~630 KB every template now carries is that item's territory)
2026-08-02 13:42 UTC — INSTRUMENT NOTE (M159), two. (1) measure-eager-bundle now DIES on a non-200 page, zero <script src>, a non-200 chunk, or an absent nav-label canary — the four ways it printed "0 eager scripts, 0.0 KB" as if it were a finding while production was serving a challenge page. The canary is checked LAST on purpose, so the void numbers stay on screen next to the reason they are void. It throws rather than calling process.exit(): exiting from inside the fetch loop tripped a libuv assertion on Windows and turned the clear message into a crash with exit code 127. (2) The probe was BASELINED against the pre-split build before it was trusted, and it failed exactly where it should — "pdf-lib is in the page's script set BEFORE any file is chosen", naming both chunks. Every green result this session is a flip from a recorded red, not an assertion that was green from the start. That is the M157 standard and it is now cheap to meet: run the new instrument against the old build before deploying.
2026-08-02 13:45 UTC — VERIFY (partial, sitemap delta only — the full daily pass ran at 01:58): sitemap 814 -> 815, none lost. The one addition is /blog/qr-code-analytics-tracking, an autopilot post, live 200. Submitted to IndexNow (200). npm run test:home-posts still 5/5, so the homepage's inlined card list is not stale — that guard is the reason a post arriving from another session cannot silently rot the most-crawled page on the site.
2026-08-02 13:50 UTC — shipped: the barcode tool's labels get their own module — ~79 KB off every barcode route (M159b, 1 commit) | pages:0 new (behaviour — /barcode, all /barcode/<type> and their RU+UZ twins) | live:yes (/barcode 724.7 -> 645.6 KB eager, /barcode/code-128 645.6, /ru/barcode/code-128 634.1 — now inside the 617-662 KB band every other template sits in. probe-barcode green in EN, RU and UZ: hydrated, every control present AND NAMED, bars painted, re-render on typing, 0 page errors. All four routes 200, self-canonical, own localized titles. Sitemap unchanged at 815, nothing to submit) | FOUND BY THE SWEEP, which is the point of having run it: BarcodeClient is a CLIENT component, so `import { barcodeTool } from "@/lib/barcode-types-i18n"` shipped the entire localized page registry — per-symbology copy, caveats and two FAQs per format in three languages, plus the type registry it filters — to every barcode route. The tool's controls reach none of it; the extracted block had zero references to the registry, so it moved whole (7.2 KB out, 50.7 KB left behind for the server pages). Third time this exact shape has cost real bytes (HOME_I18N -> nav-i18n, search-index, now this), so state it as a rule: A CLIENT COMPONENT READING ONE SLICE OF A CONTENT REGISTRY SHIPS THE WHOLE REGISTRY | guard: test:layout 33 -> 35, both mutated; the one that matters asserts the slice does not re-import what it was split from, because that failure renders identically | next: /embed/downloader second-root-layout, or the homepage split (840.7 KB, now the only page materially above the floor)
2026-08-02 13:52 UTC — SWEEP RESULT (M159, the negative finding, recorded because it closes a question rather than opening one): seventeen templates attributed on production. Fifteen sit in a 617-662 KB band — the shared floor of framework + react-dom + nav/tool labels + react-icons, with only their own page's code on top. tesseract (/pdf-tools/ocr 652.0), the AI image tools (remove-bg 649.3, upscale 649.1) and /downloader (638.3) are all ALREADY dynamic. There is no second pdf-lib hiding anywhere on the site. The two pages above the band were /barcode (724.7, fixed today) and / (840.7, which is the homepage-split item). /link-in-bio at 680.9 carries one extra 28.9 KB chunk — too small for a mission, worth a look for anyone already in that file. Re-run the sweep after a mission that adds a template, not on a schedule.
2026-08-04 17:31 UTC — VERIFY: ok — first pass of the UTC day (no session ran Aug 3, so this covers the gap). 10 recently-shipped URLs all 200 + self-canonical + own non-homepage title. robots.txt still serves "Disallow: /p$", not the bare "/p". Sitemap 815, +1 vs the Aug 2 baseline of 814, none lost — /blog/qr-code-analytics-tracking, already live, re-submitted to IndexNow (200) because the stored baseline had not been advanced. Vendor sources: 24 sources / 50 markers, 0 moved, 0 unreachable, 0 stale. No mitigation header on production — the Aug 2 Vercel challenge is long clear.
2026-08-04 18:26 UTC — shipped: the homepage's copy registries served twelve languages to an audience that reads three (M160, 36b9f9c) | pages:0 new (behaviour — /, and HomeFaq/NewsletterSection wherever they render) | live:yes (eager set 842.0 -> 776.1 KB, -65.9 KB / -7.8%; the home-i18n marker flipped YES -> no; the 170.4 KB data chunk is now 103.3 KB. probe:home-i18n drove production in real headless Chrome: en/de/ja/zh each render their OWN copy, header hydrated, 0 page errors. Sitemap unchanged at 815, nothing to submit) | THE FIND: lib/home-i18n.ts (56.2 KB) and lib/home-faq-i18n.ts (29.6 KB) held zh hi es ar fr pt id de ja tr ur bn — not one of them en, ru or uz, which are authored inline. Three client components merged one slice each at module scope, so every visitor downloaded 85.8 KB of copy it could not read while Yandex sends 70% of referrals and UZ users are the stickiest. Fourth instance of A CLIENT COMPONENT READING ONE SLICE OF A CONTENT REGISTRY SHIPS THE WHOLE REGISTRY, and the first where there was a SECOND registry hiding behind the first — scope found it, not the plan | design note worth keeping: the split ships NO aggregate module. One would be a single stray import from undoing everything, and "no aggregate exists" beats "a guard says nobody imports the aggregate". Node-side tools read the directory from scripts/, which no browser can reach. The boundary is structural, not asserted | guard: test:home-i18n, 13 assertions, 7 mutations verified; one escaped the first draft (matching the identifier `loadHomeUi` rather than a call, which a file that imports and never calls still satisfies) and that is recorded in the test | next: M161, the stale translations this scoping uncovered
2026-08-04 18:42 UTC — shipped: the honesty pass never reached the translations (M161, 7e4bec2) | pages:0 new (content — the homepage FAQ in 15 languages) | live:yes (probe:home-i18n, 8/8 languages incl. the RTL pair ar/ur: the corrected answer is in the DOM, hero + FAQ render in each language, header hydrated, 0 page errors) | THE DEFECT: M143 corrected the English answer to FAQ 2 on Jul 28; the twelve translations sat in a separate file nobody touched, so for a week they told readers in twelve languages that their files NEVER leave the device. False — /pdf-to-word's best-quality mode uploads, as do the AI and video tools — and it is the one claim most likely to decide whether someone submits a sensitive document. The three AUTHORED languages were stale the other way, still naming PDF compress as server-side after M127 moved compression fully on-device. So the English M143 "corrected" was itself wrong by the time it shipped: CHECK THE CLAIM AGAINST THE CODE, NOT AGAINST THE LAST VERSION OF THE SENTENCE | the real bug was that nothing linked the English to its translations, so test:home-i18n now fingerprints the authored English FAQ — change it and the test stays red until the twelve are revisited | GUARD-WRITING LESSON: the first draft of the second assertion banned the old sentence per language and FAILED ON THE CORRECTED COPY, because several correct answers still contain that clause, now qualified ("most tools run in the browser, SO files do not leave — however some use a server"). A substring cannot tell a false claim from a qualified one. Assert the EXCEPTION is present instead; the old answers never mentioned a server, every honest one must | next: M162
2026-08-04 18:44 UTC — shipped: twelve languages had an English heading over a localized subtitle (M162, 3101c94) | pages:0 new (behaviour — the homepage hero card in 12 languages) | live:yes (8/8 in probe:home-i18n; de "Erstelle deinen QR-Code", zh "创建你的 QR 码", ja "QRコードを作成" all rendering where "CREATE QR CODE" used to be. Homepage eager set 776.1 -> 776.4 KB, so the fix is free. Sitemap unchanged at 815) | the heading was `lang==="uz"?…:lang==="ru"?…:"CREATE QR CODE"`, so everything outside those three fell through to English on the most-crawled page on the site, while `cardTitle` sat translated in all fifteen languages and rendered in none. en/ru/uz keep their short authored forms — the CSS uppercases already and the full string wraps the narrow card on a phone — so nothing changes visually for the current audience | FOUND BY ASSERTING A STRING THE PAGE OUGHT TO RENDER: the probe's first draft checked cardTitle everywhere and reported the ENGLISH CONTROL as broken. English does not go through the copy loader, so a failing control means the instrument is accusing itself. Chasing that found a key translated fifteen times and rendered zero times. Point this at the other registries | guard: test:home-i18n 13 -> 14, asserting the heading reads t.cardTitle AND that no `lang === "ru"` test survives in that markup — the SHAPE is the defect, because a ternary over `lang` is a language list nobody remembers to extend. Mutation verified by restoring the original ternary verbatim
2026-08-04 18:46 UTC — INSTRUMENT NOTE (M162), and it nearly cost a correct commit. probe:home-i18n reported de and zh as missing their heading while ja and en passed, twice, at exactly the 25 s timeout. The page was RIGHT: dumping the element showed textContent === the expected German and Chinese exactly. The probe was measuring THE WRONG DOCUMENT. Runtime.evaluate issued straight after Page.navigate can still bind to the OUTGOING document — here /robots.txt, which has no header, no hero and no copy in any language — so the probe graded robots.txt for 25 s and called the homepage broken. That also explains the earlier "en never hydrated" and why the same language passed in one run and failed the next. Fixed by polling location.pathname + readyState until the new document has committed BEFORE evaluating, and the probe body now refuses to grade anything whose pathname is not "/" — it reports "instrument fault, not a page fault" instead. Two clean runs since, 4/4 then 8/8. THE RULE: a probe that navigates must prove which document it landed on before it reports a single assertion, and a failing CONTROL is the tell — English cannot break from a change English does not go through. Also: two background poll loops against the GitHub status API got this machine rate-limited mid-verification (the "your verification habit is itself a traffic pattern" lesson, third time). Ask production the question directly; one probe run answered what forty API polls did not.
